任务：
后端：在 Model 表/model_meta.go 中新增 context_length、max_output_tokens、knowledge_cutoff、release_date、capabilities 字段，并在 pricing 响应中下发
后端：新增 GET /api/stats/summary 接口
后端：新增 GET /api/perf/summary 接口
前端：在 types.ts 中补充 perf 摘要和 stats 摘要类型，修改 api.ts 增加新接口调用
前端：StatsHeader 改为调用 /api/stats/summary 接口，不再 mock
前端：pricing-columns.tsx 中速度/延迟/可用性列改为使用 /api/perf/summary 真实数据，mock 作为降级
改动总结：
后端（Go）
model/model_meta.go

Model 表新增 7 个元数据字段：context_length、max_output_tokens、knowledge_cutoff（varchar 16）、release_date（varchar 16）、capabilities（varchar 512）、input_modalities、output_modalities
更新 Update() 的 Select 列表，确保新字段能正确写入（包括零值）
GORM AutoMigrate 已包含 &Model{}，启动时自动建列，无需手动迁移
model/pricing.go

Pricing 结构体同步新增上述 7 个元数据字段（均带 omitempty，未设置时不下发）
updatePricing() 中从 metaMap 读取并赋值到 pricing 条目
model/log.go

新增 CountConsumedLogs(startTs, endTs) 函数，用于统计今日请求数
controller/stats.go（新文件）

新增 GET /api/stats/summary 处理函数，返回：
model_count：来自 pricing 缓存
avg_availability：过去 24h 所有模型成功率均值（来自 perf-metrics）
today_requests：今日消费日志计数
today_cost_usd：今日 quota 换算为 USD
router/api-router.go

注册 GET /api/stats/summary（无鉴权，公开）
注册 GET /api/perf/summary（无鉴权，复用 GetPerfMetricsSummary，是 /api/perf-metrics/summary 的公开别名）
前端（TypeScript/React）
features/pricing/types.ts

新增 StatsSummary、StatsSummaryResponse、ModelPerfSummary、PerfSummaryResponse 类型
features/pricing/api.ts

新增 getStatsSummary() 调用 /api/stats/summary
新增 getPerfSummary() 调用 /api/perf/summary
features/pricing/components/stats-header.tsx

用 useQuery 调用 getStatsSummary()，替代原来的 mock 固定值
接口尚未有数据时显示 —，不影响可用性
features/pricing/components/pricing-columns.tsx

PricingColumnsOptions 新增可选 perfMap?: Map<string, ModelPerfSummary>
getModelPerfStats() 优先读取 perfMap，无数据时降级到 mock（buildGroupPerformance）；perfMap 有值但该模型无数据则显示 —
features/pricing/components/pricing-table.tsx

用 useQuery 获取 getPerfSummary()，构建 perfMap，传入 usePricingColumns